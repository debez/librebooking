# Minimal pass-through overlay so we can run a runtime diagnostic via
# Railway's Custom Start Command. No image modifications. Using USER root
# so the diagnostic shell command can read everything in /etc/apache2.

FROM librebooking/librebooking:4.3.0
USER root
